//Ternary Expression.  This is a incorrect use of a ternary because I'm setting "numOrSym"
//newChar == "=" ? numOrSym = "dif" :  console.log("nothing to see hear... specifically a enter...");

//Correct use would be 
var resultofexpression = conditionasboolean ? truepart: falsepart;

//Alernative to terary expression when "else" condition isn't applicaable
//void(condition && x = true);
//void(newChar=="=" && (numOrSym = "dif"));
